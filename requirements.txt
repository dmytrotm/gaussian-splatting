# Core PyTorch stack — CUDA 12.4 builds (compatible with CUDA 12.x drivers)
# Install with: pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu124
torch==2.6.0+cu124
torchvision==0.21.0+cu124

# Numerical / image processing
numpy>=1.26,<2.0
opencv-python
Pillow

# 3D / point-cloud
plyfile

# Utilities
tqdm
joblib

# Logging (optional but used by train.py when available)
tensorboard

# Web viewer (used by viewer.py and --web_viewer flag)
viser
nerfview

# Metrics plotting (used by MetricsTracker)
matplotlib

# Local CUDA extension submodules (compiled during install)
./submodules/diff-gaussian-rasterization
./submodules/simple-knn
./submodules/fused-ssim
